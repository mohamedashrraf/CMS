import { ToggleFilterDirective } from './toggle-filter.directive';

describe('ToggleFilterDirective', () => {
  it('should create an instance', () => {
    const directive = new ToggleFilterDirective();
    expect(directive).toBeTruthy();
  });
});
